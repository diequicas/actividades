function cubo(numero) {
    let resultado = numero * numero * numero;
    if(!Number.isInteger(numero)){
    window.alert("El numero no es valido");
    return false;
    }
    let resultado = numero * numero * numero;
    return resultado;
}